TiPhone:
  regex: 'TiPhone ?([a-z0-9]+)'
  device: 'smartphone'
  model: '$1'

Apple:
  regex: 'AppleTV|iPad|iPod|iPhone'
  models:
    - regex: 'iPad'
      model: 'iPad'
      device: 'tablet'

Incomplete:
  regex: 'Incomplete'
  models:
    - regex: 'Unmatched'
      model: 'Unmatched'
      device: 'ignore'
