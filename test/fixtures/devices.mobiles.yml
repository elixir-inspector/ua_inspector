Apple:
  regex: 'AppleTV|iPad|iPod|iPhone'
  models:
    - regex: 'iPad'
      model: 'iPad'
      device: 'tablet'

Incomplete:
  regex: 'Incomplete'
  models:
    - regex: 'Unmatched'
      model: 'Unmatched'
      device: 'ignore'